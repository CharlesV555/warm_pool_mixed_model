[project]
name = "polymer-sim"
version = "0.1.0"
description = "Structured polymer reaction simulation skeleton"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "numpy",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
